#!/usesummit/sel/0.1a
# [ Get All Images // Fetch and optionally filter all images uploaded to cloud storage. ]

# SQL query to retrieve all images.
"select_images": =Query("""select * from media_store""")

# Connection to database.
"db": =Storage("database")

"select_images" -> "db"
