#!/usesummit/sel/0.1a
# [ Random Number Generator (Python) // Generate a random number between 1 and 100 using Python. ]

# Execute Python code to generate a random number.
"random_number": =Code(```python
import random
print(random.randint(1, 100))
```)

# Store the generated random number in the Response event.
"random_number_response": =Response("random_number")

# Use the generated random number for the response.
"random_number" -> "random_number_response"
